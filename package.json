{
  "name": "next-class-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "keys:generate": "node scripts/generate-vapid.mjs",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 migrations apply next-class-db --local",
    "db:remote": "wrangler d1 migrations apply next-class-db --remote"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260819.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.124.0"
  }
}
